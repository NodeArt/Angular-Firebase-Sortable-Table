export enum PeoplePriority {
    image,
    name,
    age,
    job,
    country,
    birthday,
    cv,
    favoriteFramework,
    inSearch
}

export enum EmployersPriority {
    image,
    name,
    country,
    phone,
    address,
    lookingFor,
    email
}